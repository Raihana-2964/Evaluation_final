#include <stdio.h>

int main() {
    int num1, num2, sums,sub, g;
    
    printf("Enter the first number: ");
    scanf("%d", &num1);
    
    printf("Enter the second number: ");
    scanf("%d", &num2);
    g=200;
    sums = num1 + num2;
    sub=num1-num2;
    printf("The sum of %d and %d is %d.", num1, num2, sum);
    printf("The sub of %d and %d is %d.", num1, num2, sub); 
    return 0;
}
